server:
  port: 8083
spring:
  cloud:
    config:
      server:
        git:
          default-label: configserver
          uri: https://github.com/santiagoforonda/AlmacenCarros
          search-paths: configdata
  application:
    name: Configserver
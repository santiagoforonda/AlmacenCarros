spring:
  application:
    name: PaymentService
  cloud:
    config:
      enabled: true
      uri: http://localhost:8083
management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      include:
        show-details: always